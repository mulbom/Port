#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

typedef pair<int, int> a;

int main() {
    int n;
    cin >> n;
    vector<a> s(n);
    for (int i = 0; i < n; i++) {
        cin >> s[i].first >> s[i].second;
    }
    sort(s.begin(), s.end());

    for (int i = 0; i < n; i++) {
        cout << s[i].first << " " << s[i].second << "\n";
    }

    return 0;
}
