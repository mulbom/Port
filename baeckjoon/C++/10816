#include <iostream>
#include <string>
#include <map>

using namespace std;

void SystemDefault() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
}
int main() {
	SystemDefault();

	int n, m;
	cin >> n;
	map<int, int> nc;
	int card;
	for (int i = 0; i < n; i++) {
		cin >> card;
		nc[card]++;
	}
	cin >> m;
	int tcard;
	for (int i = 0; i < m; i++) {
		cin >> tcard;
		cout << nc[tcard] << " ";
	}

	return 0;
}

//n 입력 받은 카드명의 값을 ++로 몇개가 있는지 체크
//m 입력 받은 카드명의 값을 nc에서 출력
//++되지 않은 곳이면 0 된 곳이라면 해당 값을 출력함으로써 시간제한에 걸리지않음
